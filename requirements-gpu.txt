# GPU backend (Stable Diffusion Inpainting + IP-Adapter) bağımlılıkları.
#
# CVE-2025-32434 nedeniyle torch >= 2.6 zorunlu.
# cu121 index'inde torch 2.6 yok; cu124 kullanın:
#   pip install --index-url https://download.pytorch.org/whl/cu124 "torch>=2.6" torchvision
# Ardından bu dosya:
#   pip install -r requirements-gpu.txt

diffusers>=0.27
transformers>=4.40
accelerate>=0.30
safetensors>=0.4
huggingface_hub>=0.23
peft>=0.11
